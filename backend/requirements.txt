# VocalizeAI Backend Dependencies
# Install with: pip install -r requirements.txt
# ⚠️  Piper TTS uses CLI binary (not Python package)
#     Binary is auto-downloaded on first run

# ── Web Framework ──
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# ── Speech-to-Text ──
faster-whisper

# ── Text-to-Speech ──
# Piper TTS CLI binary — auto-downloaded, no pip package needed

# ── Audio Processing ──
pydub>=0.25.1
# pydub needs ffmpeg installed on system (brew install ffmpeg)

# ── Utilities ──
numpy<2.0
spacy<3.8.0
argostranslate

# ── Voice Cloning ──
git+https://github.com/myshell-ai/OpenVoice.git
librosa
