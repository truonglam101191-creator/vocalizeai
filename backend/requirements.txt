# VocalizeAI Backend Dependencies
# Install with: pip install -r requirements.txt
# ⚠️  Requires Python 3.11 (Coqui TTS không hỗ trợ 3.12+)
#     Nếu chỉ có Python 3.12+, dùng: pip install -r requirements-lite.txt

# ── Web Framework ──
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# ── Speech-to-Text ──
faster-whisper>=1.0.0

# ── Text-to-Speech (Tiếng Việt) ──
piper-tts>=1.2.0

# ── Audio Processing ──
pydub>=0.25.1
# pydub needs ffmpeg installed on system (brew install ffmpeg)

# ── Utilities ──
numpy<2.0
