# VocalizeAI Backend - LITE version
# Dùng khi chỉ có Python 3.12+ (Coqui TTS không hỗ trợ)
# Thay TTS bằng edge-tts (cần internet cho TTS, STT vẫn offline)
# Install: pip install -r requirements-lite.txt

# ── Web Framework ──
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# ── Speech-to-Text (offline) ──
faster-whisper>=1.0.0

# ── Text-to-Speech (online - Microsoft Edge) ──
edge-tts>=6.1.0

# ── Audio Processing ──
pydub>=0.25.1

# ── Utilities ──
numpy<2.0
argostranslate
